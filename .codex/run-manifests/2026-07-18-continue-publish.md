# Headless publish run manifest

- Run date: 2026-07-18 (America/Toronto)
- Mode: `publish`
- Milestone: reconstruct the unfinished batch-clone recovery, exact tab history, and update-shutdown work represented by the two supplied Claude exports; add opt-in Docker Compose deployment to an SSH working copy after a successful app push; load the complete GitHub notification inbox and preserve the confirmed Clear all workflow; revamp the per-repository Releases workspace; then publish the integrated milestone to the default branch
- Expected UI state: Repository Settings → Remote exposes the saved SSH host, source remote, explicit deploy-after-push checkbox, and manual Docker deploy action; the notification centre exposes GitHub and Local Clear all controls and a bounded empty state against the deterministic provider (complete-list semantics are covered by the 249-item unit fixture); the repository Releases workspace presents the revamped searchable/filterable release dashboard; all changed surfaces render without a blank/black frame, private data, or visible-desktop interaction
- Ordered background interactions: preflight the fixed low-level MCP server; build through MCP; create an owned disposable Git fixture, deterministic non-secret UI state, and isolated user-data directory; create one uniquely named Win32 headless desktop; launch the freshly built Electron binary on that desktop; resolve its HWND dynamically; resize it deterministically; capture a stable client-only frame; open Repository Settings → Remote and verify the deploy controls; open the notification centre and verify the complete-list/Clear all controls; open the Releases workspace against only the deterministic provider fixture if provider data is needed; capture after meaningful interactions; save and inspect the final frame(s); close the revalidated HWND and owned desktop; remove only run-owned temporary paths
- Disposable fixture path: `C:\Users\Administrator\AppData\Local\Temp\desktop-material-p0-ui-continue-20260718-1745` (verified absent before creation); every owned path remains beneath this exact run root
- Cleanup ledger: run id `continue-20260718-1745`; Win32 desktop `DesktopMaterialContinue-20260718-1745` was created exactly once with handle `1180`; provider launcher PID `5048`, provider PID `6328`, API/Git port `51310`, and credential service `GitHub Desktop Dev - http://localhost:51310/api/v3`; earlier owned Electron PIDs `30380`, `22784`, and `7972` were terminated only after their hidden-window attempts completed; the final merged-source launch was PID `11368`, resolved main HWND `45220052`, and debug port `53381`. Final cleanup verified zero desktop windows, both ports absent, credential login `material-verifier-p0` absent after delete and independent readback, provider/launcher PIDs absent, desktop close state `closed: true`, and the exact run root removed and independently absent.
- Screenshot target: unique temporary verification PNGs beneath the run root for the SSH deploy, notification, and Releases surfaces; promote the inspected Releases frame to `docs/assets/screenshots/material-github-releases.png`
- Screenshot presentation: existing app theme from the reconstructed milestone, at a deterministic window size established from the prior evidence (fallback `1440x960` if no prior size is recorded)
- Implementation/verification allowlist: app and test files in commits `889b4641ea`, `302ca6bc7c`, `4964612bc8`, and `cc7d1a9e88`; `.codex/verification/p0_fake_github_provider.py`, `.codex/verification/test_p0_fake_github_provider.py`, `.codex/verification/probe_p0_provider.ps1`, and `.codex/verification/capture_gallery_cdp.js`; documentation files `HANDOFF.md`, `README.md`, `ROADMAP.md`, `docs/wiki/User-Guide.md`, `docs/wiki/Feature-Gallery.md`, `site/index.html`, this run manifest, and `docs/assets/screenshots/material-github-releases.png`
- Tests: reproducible `build:prod` command required by the skill; exact tab-history, update-shutdown, batch-clone recovery/concurrency, SSH working-copy/deploy-after-push, full GitHub notification pagination/Clear all, and Releases-dashboard focused tests; repository-wide tests; formatting, lint, typecheck, Git diff, and secret scan
- MCP/build receipt: `startup_status.ok=true`; scheduled task `LowLevelComputerUseMCP` runs `C:\Users\Administrator\Documents\GitHub\lowlevel-computer-use-mcp\.venv\Scripts\python.exe -m lowlevel_computer_use_mcp.server --http --host 127.0.0.1 --port 8765` from its fixed checkout at `8d6940be6a5f6e7c37de3f73acd2259fa7651efe`. The exact required command `npx --no-install cross-env RELEASE_CHANNEL=development DESKTOP_SKIP_PACKAGE=1 yarn build:prod` passed through MCP on the merged final source in `214.9s`; the npm cache supplied Yarn offline temporarily, the repository delegated it to pinned Yarn `1.21.1`, and the temporary global Yarn package was removed with `where yarn` absent.
- Provider/capture receipt: deterministic provider `http://localhost:51310/api/v3` passed its full live probe, including three release states, two stable assets, and all bounded Actions pages. Before the final relaunch, the fixture remote was changed from its `.invalid` proxy URL to the exact loopback account origin and its temporary proxy removed, allowing strict GitHub repository matching. The inspected canonical Releases frame is `1440x960`, `146835` bytes, SHA-256 `98659faa911d505cf0e1d1bfe8556bad994afd904afc959264f49ccdf6e4a856`, promoted byte-for-byte to `docs/assets/screenshots/material-github-releases.png`. Exact low-level HWND capture returned `rendered_ok: true` at `960x660`; Chromium rejected hidden-desktop background input/window actions, so the documented app-native CDP hook produced the full-size canonical frame without touching the visible desktop. The SSH deploy panel and empty GitHub notification/Clear all panel were also inspected before contained cleanup; populated pagination and mutation behavior are asserted by unit tests rather than claimed from the empty visual fixture.
- Test receipt: focused post-merge feature coverage passed `128` tests across `32` suites; the final repository run discovered `552` test files in three Windows-safe batches and exited `0` in `354.8s`; script tests passed `16/16`; provider tests passed `14/14`; provider `py_compile`, TypeScript, targeted ESLint, Prettier, and `git diff --check` passed. Packaged launch E2E remains delegated to applicable CI.
- Remote: `origin` is `https://github.com/codingmachineedge/desktop-material.git`, authenticated GitHub account `codingmachineedge`, default branch `main`; baseline was `abe607f9bfbbb5648699651a7d87246efe96cf6c`, remote work through `e859d691ef` was merged without force, and feature/evidence commit `d9cd85d6735124bde11545b61e2f923dce9830c2` was verified at `refs/heads/main` before the final handoff/cleanup receipt.
- Expected branch: repository default branch, expected `main`, with no force push
- Cleanup invariant: preserve the initial dirty baseline and all unrelated work; after remote proof, integrate all completed source tips and remove only merged/redundant temporary branches, linked worktrees, stale metadata, and stashes
- Repository cleanup receipt: linked worktree `.claude/worktrees/gracious-wilson-112500` was clean and tip `9bff08f5a71c7e63d8c590ca1db17c32153ae297` was an ancestor of pushed `origin/main`, so the worktree and local branch `claude/gracious-wilson-112500` were removed. Remote `temp-work-branch` tip `69112d637432143f7cb489db8afb9e2a3b3cee4a` was also an ancestor and was deleted without force. Remote refs and worktree metadata were pruned. The two owned export-inspection directories were moved to the Recycle Bin after exact `%TEMP%` containment checks; both supplied ZIPs remain untouched. The in-repository wiki sources were overlaid onto the separate wiki without touching its remote-only `Images` directory, pushed as `1d047329ed647d96e0bb42ed9374d89a9dbd59d2`, and the clean remotely proven publish clone was recycled. Final pre-commit audit has one `main` worktree, only local/remote `main`, no stash, and `0/0` divergence.
