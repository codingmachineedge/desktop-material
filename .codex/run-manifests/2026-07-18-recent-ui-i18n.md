# Recent UI internationalization verification manifest

- Mode: `publish`
- Milestone: internationalize the repository CI status and update-download
  progress UI added in `e0a3bca3ec`.
- Expected UI: the CI tooltip, update progress accessibility text, Appearance
  setting label, inheritance option, palette names, and CI result labels follow
  the operating-system locale with an English fallback.
- Interactions: exercise the translation catalog through deterministic unit
  tests; no visible-desktop interaction is allowed.
- Fixture: isolated locale inputs (`en`, `zh-HK`, `zh-CN`) in the unit suite.
- Screenshot target: none; translated tooltip and accessibility strings are
  non-persistent states and are covered deterministically instead.
- Documentation allowlist: this manifest, `README.md`,
  `docs/wiki/User-Guide.md`, and `HANDOFF.md`.
- Tests: focused i18n, CI status, and appearance suites; lint; full TypeScript
  `--noEmit`; `git diff --check`; attempt the exact MCP production build with a
  documented bounded fallback for environmental memory pressure; clean/pushed
  `main`; exact-SHA workflows.
- Remote/branch: `origin`, `main`.
- Initial baseline: clean `main` at
  `e0a3bca3ec94fcb94cdb6c7ac416270e8a632433`; preserve all unrelated state.
