# Notification bulk actions and error notices publish run

- Run ID: `20260716-notification-error-notices-01`
- Mode: `publish`
- Milestone: make the Local and GitHub notification sources searchable and explicitly filterable; add visible selection and bulk actions; replace the Local trash-only affordance with a labeled, visibly confirmed **Clear all** action; present generic acknowledgement-only errors as configurable, dismissible red notices in the bottom-right corner while preserving dialogs for retry, authentication, fatal, and other real decisions
- Expected notification state: deterministic synthetic Local entries cover multiple kinds, read states, accounts, and repositories; search plus kind scope narrows them; **Select all visible** selects only the filtered result; bulk mark read/unread/delete performs one durable Local mutation; GitHub selection and bulk read/done apply only to loaded visible threads and never imply unseen server rows
- Expected error state: the default **Bottom-right notice** preference shows a bounded, wrapped red error surface with a named dismiss button and an optional details action; the **Dialog** preference restores the existing generic dialog; errors with retry/options decisions remain dialogs in either mode; the persisted notification history continues receiving the safe error summary
- Ordered verification: run focused model/store/UI/preference contracts; run full TypeScript, repository-aware ESLint, Prettier, diff integrity, and the exact unpackaged production build through the fixed MCP; create an owned deterministic profile and uniquely named off-screen desktop; capture client-only notification-bulk and error-notice evidence at representative normal and short heights; inspect original pixels and computed overflow; close by revalidated HWND or saved PID fallback; remove only exact owned paths; update README, Pages, wiki, gallery contracts, and `HANDOFF.md`; commit, push, and verify main/CI/Pages/wiki
- Screenshot targets: `docs/assets/screenshots/material-notification-bulk-actions.png` and `docs/assets/screenshots/material-error-notice.png`
- Implementation allowlist: this manifest; notification models/stores/dispatcher/app state and notification UI/styles under `app/src` and `app/styles`; a focused shared error-presentation model, error notice UI/style, App/AppStore wiring, Notifications preferences, and only acknowledgement-only error handlers required for the shared path; matching focused source, Pages, and wiki-gallery tests under `app/test/unit`; owned verification helpers under `.codex/verification`; `README.md`, Pages `site/index.html`, wiki User Guide, Feature Gallery, Home, and Developer Guide, `HANDOFF.md`, and the two promoted screenshots
- Safety boundary: bulk operations target explicit IDs from the current source/context; selection is cleared or pruned on source, account, server-filter, or result changes; Local persistence is one atomic full-log write plus one history commit per bulk action; notices are transient and independent of the notification repository so a notification-history failure cannot suppress a visible error; credential-shaped values and raw unbounded output never enter notice copy
- Publication authorization: the active goal explicitly says to screenshot and always push as work progresses

## Baseline

- Default checkout: `C:\Users\Administrator\Documents\GitHub\desktop-material`
- Source HEAD and `origin/main`: `3873a2b71d29ff2eaedd334449f56d1eccd46d68`; zero divergence
- Branch/worktree/stash state: local `main`, remote `origin/main`, one default worktree, no stashes, clean before this manifest
- Prior GitHub API Explorer milestone: implementation `116f9eb315b94e62faac0f7947e26e928acd88a7`, documentation precision follow-up `3873a2b71d29ff2eaedd334449f56d1eccd46d68`, Pages runs successful, wiki `86a1dcd6108659126e2c529350e7174bfd9d8cb4`; the implementation CI and installer workflows remained in progress when this independent milestone began

## Verification receipts

- Focused notification, GitHub inbox, error-routing, preference, profile-history, responsive-style, and React interaction coverage: `68/68` passed across `21` suites.
- The final combined source, Pages, and 58-item wiki-gallery gate passed `84/84` tests across `24` suites after documentation and screenshot promotion.
- Full `tsc --noEmit`, repository-aware ESLint with the checked-in rules directory, targeted Prettier, and `git diff --check` passed after final source changes.
- Fixed MCP preflight: `startup_status.ok=true`; scheduled task `LowLevelComputerUseMCP` is running at limited privilege from `C:\Users\Administrator\Documents\GitHub\lowlevel-computer-use-mcp` with the fixed venv Python and `-m lowlevel_computer_use_mcp.server --http --host 127.0.0.1 --port 8765`.
- Exact MCP checkout revision: `8d6940be6a5f6e7c37de3f73acd2259fa7651efe`; all three preflight calls returned `client_ok=true`, and both commands returned code `0` without timeout.
- Prior API Explorer publication follow-up completed successfully: CI runs `29549686621` and `29549926806`, plus installer run `29549686589`, all concluded `success`.
- The first exact MCP build invocation failed closed before compilation because the scheduled-task environment could not resolve `yarn` (`spawn yarn ENOENT`). An ignored temporary `node_modules/.bin/yarn.cmd` shim exposed the repository's checked-in `vendor/yarn-1.21.1.js` without a download or dependency change; the required command remained exactly `npx --no-install cross-env RELEASE_CHANNEL=development DESKTOP_SKIP_PACKAGE=1 yarn build:prod`.
- The repeated exact unpackaged production build completed successfully through the fixed MCP in `131.72s` (`returncode=0`, `timed_out=false`, `client_ok=true`) for all webpack targets. The temporary Yarn shim was removed immediately afterward and is absent from the checkout.
- The isolated verifier seeded only `has-shown-welcome-flow`, light theme, auto-fit, statistics opt-out, and `error-presentation-style=notice`, then restarted the app so AppStore read the preference at construction. Its first evidence retries failed closed when a notice button detached during dismissal and when earlier synthetic notification rows contaminated an exact three-row assertion; the retained verifier now dismisses the current DOM element and clears only the disposable profile through the real **Clear all** UI before generating deterministic evidence.
- Accepted notice receipt: normal CSS viewport `1000×687`; document and body client/scroll widths all `1000`; fixed stack and its single card entirely within the viewport; card client/scroll widths both `414`; named `Dismiss Error` action; no horizontal overflow. A physical `640×480` short-device override also stayed within bounds after the app's auto-fit settled.
- Accepted notification receipt: normal panel fully within `1000×687`; short-device override settled at CSS `1029×600`; panel width/scroll width `384/384`; source width/scroll width `374/374`; source height/scroll height `473/486` with `overflow-y:auto`; list width/scroll width `365/365`; exactly three filtered error rows, mixed read state, `3 selected`, every bulk control reachable, no unnamed controls, and the non-modal **Clear all** confirmation explained notification-history recovery.
- Accepted app-native captures were reopened at original pixels and privacy-reviewed: `material-error-notice.png` is `1000×687`, `101,359` bytes, SHA-256 `953467ac7846bf01ec3090b01b15938c35e7be2ee73bd0638e1df3bfeaf3fe0b`; `material-notification-bulk-actions.png` is `1029×600`, `101,445` bytes, SHA-256 `b3ca2875c1080733e832df49bc0680e7711ad650809c33149382f96fe8cf7c32`. Both contain only the synthetic `git-source` fixture and deterministic test-error copy.
- The final MCP HWND capture was rejected as stale because its SHA-256 exactly matched the pre-interaction frame. The documented app-native CDP fallback supplied current pixels while process, port, profile, and desktop ownership stayed bounded.
- Final accepted launch PID `12760`, HWND `7406868`, CDP port `57931`, and hidden desktop `DesktopMaterialNotifyError-20260716-01` were revalidated. Generic alternate-desktop close failed closed; only the exact owned Electron process set was terminated, the listener count reached zero, the desktop listed zero windows and closed, and the retained containment-checking cleanup helper removed the exact Temp run root. Earlier seed/retry PIDs were also verified stopped.
