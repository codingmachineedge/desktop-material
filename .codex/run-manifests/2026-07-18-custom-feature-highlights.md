# Custom feature highlighting publish manifest

- Run date: 2026-07-18 (America/Toronto)
- Mode: `publish`
- Milestone: add an optional global Appearance preference that visually distinguishes Desktop Material-only features from controls inherited from original GitHub Desktop, persist it safely, apply one consistent accessible treatment to explicitly classified feature entry points, document it, and push the verified result to `main`
- Expected UI state: **Settings → Appearance** exposes a clearly described **Highlight Desktop Material features** toggle; default/off preserves the existing neutral UI; on adds a restrained Material accent and a compact `M` or textual `Material` marker to classified custom feature entry points without changing selection, disabled state, keyboard behavior, or upstream controls
- Ordered background interactions: preflight the fixed low-level MCP server; build through MCP; create an owned disposable Git/provider fixture and isolated user-data directory; create one uniquely named Win32 headless desktop; launch the freshly built Electron binary only there; resolve its HWND dynamically; capture a stable frame; open Appearance, enable the toggle, save, inspect highlighted custom navigation/settings entry points, and capture the final Appearance state; close the revalidated HWND and desktop; remove only owned temporary paths
- Disposable fixture path: `C:\Users\Administrator\AppData\Local\Temp\desktop-material-p0-ui-highlight-20260718-1922` (must be absent before creation); every owned path must remain beneath this exact root
- Cleanup ledger: run id `20260718-1922-custom-feature-highlights`; Win32 desktop `DesktopMaterialHighlight-20260718-1922` was created once; provider PID `1384` on loopback port `57756`; launch PID `10444`; resolved HWND `77071374`; no credential was installed; the hidden-HWND close helper could not resolve an off-screen handle, so the revalidated exact launch PID alone was terminated as the documented fallback; the desktop then reported zero windows and closed; exact provider PID `1384` stopped; CDP port `9346` had zero listeners; containment-checked cleanup removed the exact owned run root
- Screenshot target: refresh `docs/assets/screenshots/material-customization.png` only after an inspected original-resolution Appearance capture shows the new preference and no private data, blank/black pixels, or clipping
- Screenshot presentation: existing light theme and deterministic `1440x960` documentation viewport, with a separate exact-HWND low-level capture as transport evidence
- Implementation/verification allowlist: the existing appearance schema/theme bridge, preferences Appearance, explicit repository/repository-tab/notification/settings/toolbar/menu/command-palette entry points, one centralized menu/command classification allowlist, Material styles, focused unit/style tests, `README.md`, `ROADMAP.md`, `docs/wiki/Home.md`, `docs/wiki/User-Guide.md`, `docs/wiki/Developer-Guide.md`, `docs/wiki/Feature-Gallery.md`, `site/index.html`, `HANDOFF.md`, this manifest, the deterministic capture helper, and the single promoted screenshot
- Tests: focused persistence/history/render/style/classification coverage; repository-wide unit and script tests; TypeScript; scoped ESLint/Prettier; exact MCP production build; original-resolution headless inspection; diff and staged-secret scans; applicable GitHub CI including packaged E2E
- Remote: `https://github.com/codingmachineedge/desktop-material.git`, authenticated account `codingmachineedge`, default/expected branch `main`, baseline and initial `origin/main` `604c3975dbd172ca8821e90ea514b0f8f3270440`, no force push
- Initial repository state: clean `main`, one worktree, only local/remote `main`, no stash, `0/0` divergence; preserve both user-supplied ZIP files and create no linked worktree
- Cleanup invariant: after remote proof, leave one clean `main` checkout at the exact pushed remote SHA, no staged/unstaged/untracked work, no stash, no temporary branch/worktree, and no owned headless or fixture resource

## Implementation and local verification receipts

- The new `highlightDesktopMaterialFeatures` boolean is profile-scoped inside the existing version-1 appearance document, defaults to `false`, accepts only real booleans, participates in the existing local profile Git history, and is deliberately excluded from repository-local overrides. No schema version or storage key changed.
- `AppTheme` owns a single `data-dm-highlight-features` body gate and removes it on false/unmount. All marker styling is inert outside that gate and contains no new animation.
- The reviewed marker inventory is conservative: repository Actions, Releases, Issues, API, Triage, and Tools; repository-tab search, arrange, new-tab, notification, and settings-history controls; Settings Agent access and Automation; Commit & Push, Build & Run, and theme toolbar entries including overflow; and a centralized allowlist of custom app-menu/command-palette entry points. Changes, History, Branches, Accounts, Git, Appearance, and other mixed/upstream controls are intentionally unmarked.
- Fixed MCP preflight returned `startup_status.ok=true`; scheduled task `LowLevelComputerUseMCP` runs `C:\Users\Administrator\Documents\GitHub\lowlevel-computer-use-mcp\.venv\Scripts\python.exe -m lowlevel_computer_use_mcp.server --http --host 127.0.0.1 --port 8765` from the fixed checkout at `8d6940be6a5f6e7c37de3f73acd2259fa7651efe`.
- The first exact build request stopped before compilation because the temporary Yarn shim was absent. Yarn `1.22.22` was restored strictly from an already populated local npx cache; the repository delegated it to pinned Yarn `1.21.1`; the identical required MCP command then returned `ok=true`, `returncode=0`, `timed_out=false`, and `client_ok=true` in `280.7s`, building to `out`. After the marker inventory was expanded, a second fixed-MCP preflight and identical exact command rebuilt the final source successfully in `229.1s` (`yarn` reported `227.46s`). Both temporary shims came only from that cache, were removed immediately, and left `where yarn` absent.
- Focused persistence, Appearance UI, body-gate cleanup, explicit-marker/responsive-style, shell composition, Settings toggle, and allowlist coverage passed `28/28` across seven files. All `554` repository unit-test files passed in three batches in `516.9s`; script tests passed `16/16`; deterministic provider tests passed `14/14`; root and script TypeScript, repository-wide no-cache ESLint, and repository-wide Prettier passed. The configured Markdownlint command is not a clean repository gate on Windows: its recursive glob includes nested dependencies and reported `54,084` baseline diagnostics, so no Markdownlint pass is claimed.
- The final accepted light capture is `1440x960`, `137,390` bytes, SHA-256 `3e7cfc236741dc9873e4e3dace1d25e58b57c73464ed026a45e56c44eda53b08`. It shows the enabled switch, full `Material` badges on Agent access/Automation, compact `M` markers on fork-only repository rail, tab-strip/notification, and toolbar controls, synthetic fixture data only, and no blank pixels or clipping. The promoted tracked file is byte-identical.
- A separate final `1440x960` dark capture (`136,579` bytes) showed legible selected, switch, edge, and badge states. A `600x240` shell capture (`34,525` bytes) showed unclipped compact rail/tab-strip markers, and a true narrow Settings capture at `300x400` transport pixels (`32,463` bytes, `600x800` CSS viewport) showed the 72px icon-only rail using compact `M` badges without shrinking or overflow. All temporary evidence files were inspected and removed with the owned run root.
- Final run `20260718-2015-custom-feature-highlights-final` used desktop `DesktopMaterialHighlightFinal-20260718-2015` (handle `1044`), provider/launcher PIDs `32220`/`20420` on loopback port `52821`, launch PID `7260`, runtime HWND `20251006`, and CDP port `9347`. The hidden-HWND close helper failed closed, so the revalidated exact Electron PID alone was terminated; the desktop reached zero windows and closed, both ports reached zero listeners, both provider processes stopped, and containment-checked cleanup removed the exact owned Temp root. The visible desktop and unrelated Electron processes were untouched.
- Diff/staged scans, commit/push, remote CI, wiki publication, and final branch/worktree/stash cleanup receipts remain to be completed below before this manifest is final.
